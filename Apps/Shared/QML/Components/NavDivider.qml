import QtQuick
import QtQuick.Layouts
import Silicium.Theme

Rectangle {

    id: divider

    width: 1
    Layout.fillHeight: true
    Layout.topMargin: 5
    Layout.bottomMargin: 5
    color: Palette.toolbarBorder

    /*
    Rectangle {

        anchors.left: parent.right
        width: 1
        height: parent.height
        color: Palette.surface.darker()
    }

     */
}